const express = require('express');
const router = express.Router();

// Este archivo puede servir como punto de entrada para otras rutas API
// Por ahora está limpio ya que las rutas específicas están en sus propios archivos

module.exports = router; 